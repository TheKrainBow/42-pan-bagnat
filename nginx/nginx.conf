worker_processes  auto;
error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;
    # Keep default client_max_body_size; override per upload endpoints only

    resolver 127.0.0.11 valid=10s ipv6=off;
    resolver_timeout 5s;

    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    upstream pan-bagnat-frontend {
        zone pan_bagnat_frontend 64k;
        server pan-bagnat-frontend:3000 resolve;
    }

    upstream pan-bagnat-backend {
        zone pan_bagnat_backend 64k;
        server pan-bagnat-backend:8080 resolve;
    }

    upstream pan-bagnat-modules-proxy {
        zone pan_bagnat_modules_proxy 64k;
        server pan-bagnat-modules-proxy:9090 resolve;
    }

    server {
        listen 443 ssl;
        server_name localhost heinz.42nice.fr panbagnat.42nice.fr;

        ssl_certificate     /etc/ssl/fullchain.pem;
        ssl_certificate_key /etc/ssl/42nice.fr.key;

        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_ciphers         HIGH:!aNULL:!MD5;

        location = /healthz {
            access_log off;
            default_type text/plain;
            return 200 'ok';
        }

        # Allow large bodies only for icon upload endpoints
        location ~ ^/api/v1/admin/modules/[^/]+/icon/upload$ {
            client_max_body_size 50m;
            proxy_request_buffering off;
            proxy_pass         http://pan-bagnat-backend;
            proxy_http_version 1.1;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_set_header   X-Forwarded-Host  $host;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_redirect     off;
        }
        location ~ ^/api/v1/admin/modules/[^/]+/pages/[^/]+/icon/upload$ {
            client_max_body_size 50m;
            proxy_request_buffering off;
            proxy_pass         http://pan-bagnat-backend;
            proxy_http_version 1.1;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_set_header   X-Forwarded-Host  $host;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_redirect     off;
        }

        location /api/ {
            proxy_pass         http://pan-bagnat-backend;
            proxy_http_version 1.1;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-Proto $scheme;      # NEW
            proxy_set_header   X-Forwarded-Host  $host;        # NEW
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for; # NEW
            proxy_redirect     off;                             # optional
        }

        location /auth/ {
            proxy_pass         http://pan-bagnat-backend;
            proxy_http_version 1.1;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-Proto $scheme;      # NEW
            proxy_set_header   X-Forwarded-Host  $host;        # NEW
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for; # NEW
            proxy_redirect     off;                             # optional
        }

        location /ws {
            proxy_pass         http://pan-bagnat-backend;
            proxy_http_version 1.1;
            proxy_set_header   Upgrade $http_upgrade;
            proxy_set_header   Connection $connection_upgrade;  # changed
            proxy_read_timeout 600s;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-Proto $scheme;       # NEW
            proxy_set_header   X-Forwarded-Host  $host;         # NEW
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for; # NEW
        }

        location ^~ /module-page/ {
            proxy_pass         http://pan-bagnat-modules-proxy;

            proxy_hide_header Access-Control-Allow-Origin;
            add_header Access-Control-Allow-Origin $http_origin always;

            proxy_http_version 1.1;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_set_header   X-Forwarded-Host  $host;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_redirect     off;
        }

        # Serve backend-uploaded icons only; let the frontend own other /assets/* (e.g., CSS)
        location ^~ /assets/module-icons/ {
            proxy_pass         http://pan-bagnat-backend;
            proxy_http_version 1.1;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_set_header   X-Forwarded-Host  $host;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_redirect     off;
        }
        location ^~ /assets/page-icons/ {
            proxy_pass         http://pan-bagnat-backend;
            proxy_http_version 1.1;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_set_header   X-Forwarded-Host  $host;
            proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_redirect     off;
        }

        location / {
            proxy_pass         http://pan-bagnat-frontend;
            proxy_http_version 1.1;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
        }
    }

    server {
        listen 80;
        server_name localhost heinz.42nice.fr panbagnat.42nice.fr;

        location = /healthz {
            access_log off;
            default_type text/plain;
            return 200 'ok';
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }
}
